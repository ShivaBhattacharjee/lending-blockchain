import AppleCards from "@/components/cards";
import React from "react";

const page = () => {
  return (
    <div className=" min-h-screen">
      <AppleCards />
    </div>
  );
};

export default page;
